# TFG-Backreaction-LA
En este repositorio se engloban el código empleado y las imágenes obtenidas con él. Para la ejecución del código, se recomienda ejecutarlo todo en orden, ya que hay variables que cambian de forma durante el código, por lo que ejecutar celdas anteriores a otras puede suponer problemas o imposibilitar la compilación.
